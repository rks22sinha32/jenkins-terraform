pipeline {
    agent any

    stages {
        stage('terrafrom init') {
            steps {
                sh 'terraform init'
            }
        }
        stage('terraform plan') {
            steps {
                sh 'terraform plan'
            }
        }
        stage('terraform apply') {
            steps {
                sh 'terraform ${ation} -auto-approve'
            }
        }
    }
}